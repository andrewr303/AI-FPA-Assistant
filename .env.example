# Optional: URL of an external Copilot API (e.g. Cloudflare Worker, Vercel function)
# that exposes POST /ask-finance and POST /variance-brief.
# Leave unset to run in static demo mode.
VITE_COPILOT_API_URL=
